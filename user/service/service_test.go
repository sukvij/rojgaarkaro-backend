package service

import (
	// mocks "fitcare-backend/user/service/mocks"
	"testing"
)

func TestGetAllUsers(t *testing.T) {
	// mockControll := gomock.NewController(t)
	// mockuser := mocks.NewMockServiceMethod(mockControll)
	// user := &userModel.User{FirstName: "vijju"}
	// mockuser.EXPECT().GetAllUsers().Return(user)

}
